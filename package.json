{
  "name": "playbook-editor",
  "version": "1.0.0",
  "private": true,
  "description": "CRUD manager and relationship map for LLM diagnostic/triage playbooks — dialog steps, routing logic, and everything else in the surrounding XML.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.21",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.8"
  }
}
