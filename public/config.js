// Placeholder for local dev / static hosts. In the Cloud Run container this
// file is overwritten at startup from environment variables — see
// deploy/docker-entrypoint.sh.
window.__APP_CONFIG__ = window.__APP_CONFIG__ || {};
